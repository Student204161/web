from setuptools import setup, find_packages

setup(
    name="lightglue",
    version="0.1",
    description="LightGlue: Lightweight feature matching.",
    packages=find_packages(),
    install_requires=[],
)
